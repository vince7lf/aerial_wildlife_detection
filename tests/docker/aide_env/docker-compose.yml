version: '3.7'
services:
  aide_app:
    hostname: 'aide_app_host' 
    build:
      context: '../..'
      dockerfile: 'Dockerfile'
    image: 'test_docker_aide_app'
    environment:
      - AIDE_HOST_ENV_TAG=${AIDE_HOST_ENV_TAG}

# AIDE_HOST_ENV_TAG=vbox docker-compose up -d